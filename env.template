# Track 后端服务环境变量配置示例
# 复制此文件为 .env 并修改相应的值

# ==================== 数据库配置 ====================
# PostgreSQL 数据库名称
POSTGRES_DB=track

# PostgreSQL 用户名（建议修改为自定义用户名）
POSTGRES_USER=trackuser

# PostgreSQL 密码（⚠️ 生产环境请务必修改为强密码）
POSTGRES_PASSWORD=change_this_password_123

# ==================== Redis 配置 ====================
# Redis 主机地址（Docker 内部使用服务名）
REDIS_HOST=redis

# Redis 端口
REDIS_PORT=6379

# ==================== 应用配置 ====================
# Spring Boot 配置文件（默认：default，生产环境可以设置为：prod）
SPRING_PROFILES_ACTIVE=default

# Webhook 功能开关（true/false）
WEBHOOK_ENABLED=false

# ==================== JVM 配置 ====================
# Java 虚拟机参数
# 默认：最大堆内存 1GB，初始堆内存 512MB
# 如果服务器内存较小，可以调整为：-Xmx512m -Xms256m
JAVA_OPTS=-Xmx1g -Xms512m

# ==================== 端口配置 ====================
# 应用服务端口（默认：8080）
# 如果 8080 端口被占用，可以修改为其他端口，如：9000
SERVER_PORT=8080

# ==================== 安全提示 ====================
# ⚠️ 生产环境部署前请务必：
# 1. 修改 POSTGRES_PASSWORD 为强密码
# 2. 修改 POSTGRES_USER 为自定义用户名
# 3. 不要将 .env 文件提交到 Git 仓库
# 4. 定期备份数据库数据

