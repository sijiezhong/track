# 本地开发环境配置文件示例
# 复制此文件为 .env.local 并根据需要修改
# .env.local 不会被提交到 git

# Mock 开关：设置为 "true" 启用本地 mock 数据（仅开发环境）
# 启用 mock 后，不会使用 proxy 或真实 API
VITE_ENABLE_MOCK=false

# 代理目标地址（如果使用 proxy 方式）
# 启用 mock 时不需要配置此项
# 示例：VITE_PROXY_TARGET=https://your-api-domain.com
VITE_PROXY_TARGET=

# 或者直接使用 API 基础地址（如果设置了，则不会使用 proxy 和 mock）
# 启用 mock 时不需要配置此项
# 示例：VITE_API_BASE_URL=http://localhost:8080
# VITE_API_BASE_URL=
